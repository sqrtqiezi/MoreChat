# language: zh-CN
功能: 消息发送与实时功能
  作为一个已登录用户
  我想要发送消息并实时接收新消息
  以便与联系人沟通

  背景:
    假设 我访问应用首页
    假设 我已经成功登录
    当 我访问页面 "/chat"

  场景: 未选择会话时显示引导空状态
    那么 应该显示未选择会话的引导空状态

  场景: 选择预置会话后输入框启用
    假设 我选择了名为 "E2E Messaging Peer" 的会话
    那么 消息输入框应该可用

  场景: 在已有会话中发送文本消息成功
    假设 我选择了名为 "E2E Messaging Peer" 的会话
    当 我在消息输入框输入 "你好，这是一条测试消息"
    那么 发送按钮应该可用
    当 我点击发送按钮
    那么 我应该在聊天窗口看到消息 "你好，这是一条测试消息"
    那么 消息输入框应该被清空

  场景: 通过 webhook 接收新消息并显示在当前会话
    假设 我选择了名为 "E2E Messaging Peer" 的会话
    当 服务器通过 webhook 推送一条新消息
    那么 我应该在聊天窗口看到新消息
