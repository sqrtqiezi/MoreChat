# language: zh-CN
功能: 消息发送与实时功能
  作为一个已登录用户
  我想要发送消息并实时接收新消息
  以便与联系人沟通

  背景:
    假设 我访问应用首页
    假设 我已经成功登录
    假设 消息发送 API 已被 mock
    当 我访问页面 "/chat"

  @mock-api
  场景: 发送文本消息
    假设 我选择了一个会话
    当 我在消息输入框输入 "你好，这是一条测试消息"
    那么 发送按钮应该可用
    当 我点击发送按钮
    那么 消息输入框应该被清空

  @mock-api
  场景: 空消息不能发送
    假设 我选择了一个会话
    那么 发送按钮应该不可用

  @mock-api
  场景: 未选择会话时输入框禁用
    那么 消息输入框应该被禁用

  @mock-api
  场景: 通过 WebSocket 接收新消息
    假设 我选择了一个会话
    当 服务器推送一条新消息
    那么 我应该在聊天窗口看到新消息

